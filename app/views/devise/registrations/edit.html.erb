<h2>Editing User</h2>

<%= render 'user' %>

<%= form_for(@user, :url => {:controller => :registrations, :action => :update})  do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%#
  <%= @user.id  <%= @user.name 
  <input type="hidden" name="user[id]" value="<%= current_user.id
  <%= hidden_field current_user.id, 'id' %>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Top', root_path %>
